name: Build CI - Windows

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:

    runs-on: Windows

    steps:
    - uses: actions/checkout@v3
    - name: Build
      run: make -C 02_implementation/unity/

